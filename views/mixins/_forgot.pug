mixin forgotForm()
  form.form(action="/account/forgot" method="POST")
    h2 Я забыл свой пароль!
    label(for="email") Email
    input(type="email" name="email")
    input.button(type="submit" value="Отправить письмо на почту")
